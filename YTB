<!DOCTYPE html>
<
        // Когда плеер готов
        function onPlayerReady(event) {
            event.target.playVideo();
        }

        // Обработка состояния плеера
        function onPlayerStateChange(event) {
            if (event.data === YT.PlayerState.ENDED) {
                // Видео завершено
                alert('Видео завершено');
            }
        }
    </script>

    <!-- Вставьте следующий скрипт для загрузки плеера YouTube API -->
    <script src="https://www.youtube.com/iframe_api"></script>
</body>
</html>

Ps.SIAM
<!DOCTYPE html>
<
        // Когда плеер готов
        function onPlayerReady(event) {
            event.target.playVideo();
        }

        // Обработка состояния плеера
        function onPlayerStateChange(event) {
            if (event.data === YT.PlayerState.ENDED) {
                // Видео завершено
                alert('Видео завершено');
            }
        }
    </script>

    <!-- Вставьте следующий скрипт для загрузки плеера YouTube API -->
    <script src="https://www.youtube.com/iframe_api"></script>
</body>
</html>

Ps.SIAM
<!DOCTYPE html>
<
        // Когда плеер готов
        function onPlayerReady(event) {
            event.target.playVideo();
        }

        // Обработка состояния плеера
        function onPlayerStateChange(event) {
            if (event.data === YT.PlayerState.ENDED) {
                // Видео завершено
                alert('Видео завершено');
            }
        }
    </script>

    <!-- Вставьте следующий скрипт для загрузки плеера YouTube API -->
    <script src="https://www.youtube.com/iframe_api"></script>
</body>
</html>

Ps.SIAM
<!DOCTYPE html>
<
        // Когда плеер готов
        function onPlayerReady(event) {
            event.target.playVideo();
        }

        // Обработка состояния плеера
        function onPlayerStateChange(event) {
            if (event.data === YT.PlayerState.ENDED) {
                // Видео завершено
                alert('Видео завершено');
            }
        }
    </script>

    <!-- Вставьте следующий скрипт для загрузки плеера YouTube API -->
    <script src="https://www.youtube.com/iframe_api"></script>
</body>
</html>

Ps.SIAM
